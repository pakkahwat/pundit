// ลูกบอลหมุนขนาดเล็ก สำหรับใส่ในปุ่มตอนกำลังทำงาน
//
// ใช้คนละตัวกับ PitchLoader ที่เป็นบอลกลิ้งบนสนาม เพราะในปุ่มมีที่แค่ไม่กี่พิกเซล
// อนิเมชันกลิ้งข้ามสนามจะไม่มีทางอ่านออก ในพื้นที่เล็กแบบนี้ "หมุนอยู่กับที่" สื่อได้ชัดกว่า
//
// ใช้สีขาว-ดำตายตัว ไม่ใช้ currentColor เพราะตัวนี้ไปโผล่บนปุ่มหลายสี (เขียวทึบ, แท็บโปร่งใส,
// ลิงก์ในเมนู) ถ้าตามสีข้อความของแต่ละที่ บางที่จะจางจนมองไม่เห็น — และลูกฟุตบอลจริงก็ขาว-ดำ
// อยู่แล้ว จึงอ่านออกว่าเป็นลูกบอลทันทีทั้งธีมสว่างและมืด
//
// ลายเรียบกว่าลูกใหญ่ใน PitchLoader มาก เพราะที่ 18px ลายละเอียดจะกลายเป็นจุดพร่า ๆ
// เหลือแค่ห้าเหลี่ยมกลางกับอีกสองอันข้าง ๆ ซึ่งพอให้เห็นว่ามันหมุนจริง (วงกลมล้วนหมุนแล้วดูนิ่ง)
export function BallSpinner({ className = '' }: { className?: string }) {
  return (
    <svg
      viewBox="0 0 32 32"
      className={`h-[18px] w-[18px] animate-spin ${className}`}
      aria-hidden
      style={{ animationDuration: '0.8s' }}
    >
      <circle cx="16" cy="16" r="14.5" fill="#ffffff" stroke="#0f172a" strokeWidth="2" />
      <path d="M16 7.5l5.4 3.9-2.1 6.4h-6.6l-2.1-6.4z" fill="#0f172a" />
      <path d="M30 13.5l-1.4 4.6-2.6-.6-1.1-4.8z" fill="#0f172a" />
      <path d="M2 13.5l5.1-.8-1.1 4.8-2.6.6z" fill="#0f172a" />
    </svg>
  );
}
